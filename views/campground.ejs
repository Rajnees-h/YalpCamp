

<%- include('partials/header') %>

<div class="container">
    <header class="jumbotron">
        <div class="container">
            <h1>Welcome to Camp Ground</h1>
            <p>Find your best campground here....</p>
            <p>
                <a class="btn btn-primary btn-large" href="/campground/new">Add New Campground</a>
            </p>     
        </div>
    </header>

    <!-- <div class="row">
        <div class="col-sm-12">
            <p>There are aur awesomes campground Pics....</p>
        </div>
    </div> -->

    <div class="row text-center" style="display: flex; flex-wrap: wrap;">
        <% campgrounds.forEach(function(campground){ %>
            <div class="col-md-3 col-sm-6">
                   <div class="thumbnail">
                    <img src=" <%= campground.image %> " alt="IMG">
                    <p class="img-caption"> <%= campground.name %> </p>
            
                   </div>
            </div>
        <% }); %>
    </div>
</div>

<%- include('partials/footer') %>